# ============================================================================
# MULTIDOMAIN ADDITIONS — for Cisco Live LTRXAR-3100 multidomain demo
#
# This file is COMMENTED OUT by default. Uncomment the block below to
# activate this resource as part of the multidomain rollout.
#
# To uncomment: select the YAML block in your editor and toggle comments
# (Cmd/Ctrl+/ in VS Code), or run from a shell:
#   sed -i 's/^# //' <this-file>
# ============================================================================

# ---
# # Multi-domain hub device template.
# # Composes the base feature templates from sdwan/ with the handoff-specific
# # templates declared in this repo. One device template is reused by both
# # C-EDGE-01 (Site 1, Fabric B-facing) and C-EDGE-02 (Site 2, Fabric A-facing).
# #
# # VPN 40 is intentionally NOT bound here — neither cEdge has a VPN 40
# # handoff in this design (Campus VN and IoT VN both share VPN 30). The
# # VPN 40 feature template still exists in sdwan/ as part of the foundation
# # library for future use.
# sdwan:
#   edge_device_templates:
#     - name: DT-HUB-C8000V-MULTIDOMAIN
#       description: "Multidomain HUB C8000V - (SDA fabric border handoff over VPN 30)"
#       device_model: C8000V
#       aaa_template: FT-REMOTE-EDGE-AAA-01
#       banner_template: FT-REMOTE-EDGE-BANNER-01
#       bfd_template: FT-REMOTE-EDGE-BFD-01
#       cli_template: FT-REMOTE-EDGE-CLI-BASE-01
#       global_settings_template: FT-REMOTE-EDGE-GLOBAL-01
#       logging_template: FT-REMOTE-EDGE-LOGGING-01
#       ntp_template: FT-REMOTE-EDGE-NTP-01
#       omp_template: FT-REMOTE-EDGE-OMP-01
#       security_template: FT-REMOTE-EDGE-SECURITY-01
#       snmp_template: FT-REMOTE-EDGE-SNMPV3-01
#       system_template: FT-REMOTE-EDGE-SYSTEM-01
#       vpn_0_template:
#         name: FT-REMOTE-VPN0-OVERLAY
#         ethernet_interface_templates:
#           - name: FT-TLOC1-PUBLIC-REMOTE-VPN0
#           - name: FT-TLOC2-PRIVATE-REMOTE-VPN0
#       vpn_service_templates:
#         - name: FT-REMOTE-VPN30-CORP
#           bgp_template: FT-BGP-VPN30-HANDOFF
#           ethernet_interface_templates:
#             - name: FT-ETH3-SUBIF-VPN30-HANDOFF
#       vpn_512_template:
#         name: FT-REMOTE-VPN512-MGMT
